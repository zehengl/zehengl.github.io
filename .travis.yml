language: ruby
rvm:
- 2.1


script: 
 - bundle exec jekyll build
 - bundle exec htmlproof ./_site --href-ignore "#", https://linkedin.com/in/zehengl

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

